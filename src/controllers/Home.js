import React from "react";
import { View, Text } from "react-native";

const Home = () => {

    return (
        <View style={{alignContent: "center", backgroundColor: "black"}}>
            <Text>Welcome iMran to React Native World.....</Text>
        </View>
    );
};

export default Home;